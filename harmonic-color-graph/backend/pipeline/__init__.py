"""Offline data pipeline (corpus ingestion, vocab/coverage reports, and
later: aggregation, ngrams, patterns, color, embeddings, snapshot export).

Not part of the runtime API — see pyproject.toml's `[pipeline]` /
`[ml]` extras and `feature-specs/v2-implementation-plan.md` §1.
"""
